{
  "name": "pangpang-anipang",
  "private": true,
  "sideEffects": false,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 5174",
    "build": "vite build",
    "build:dev": "vite build --mode development",
    "deploy:cloudflare": "npm run build && npx wrangler --cwd dist pages deploy . --project-name pangpang-anipang --branch production",
    "deploy:cloudflare:firebase": "VITE_FIREBASE_ENABLED=true VITE_FIREBASE_USE_FUNCTIONS=false npm run build && npx wrangler --cwd dist pages deploy . --project-name pangpang-anipang --branch production",
    "deploy:firebase": "npm run functions:build && npx firebase-tools deploy --only firestore:rules,functions --project pangpnag-c999d",
    "deploy:firebase:rules": "npx firebase-tools deploy --only firestore:rules --project pangpnag-c999d",
    "functions:build": "npm --prefix functions run build",
    "functions:lint": "npm --prefix functions run lint",
    "preview": "vite preview --host 127.0.0.1 --port 4174",
    "typecheck": "tsc --noEmit",
    "lint": "eslint .",
    "verify": "npm run lint && npm run typecheck && npm run build && npm run functions:build && npm run functions:lint",
    "format": "prettier --write ."
  },
  "dependencies": {
    "firebase": "^12.13.0",
    "react": "^19.2.0",
    "react-dom": "^19.2.0",
    "tailwindcss": "^4.2.1",
    "tw-animate-css": "^1.3.4"
  },
  "devDependencies": {
    "@eslint/js": "^9.32.0",
    "@tailwindcss/vite": "^4.3.0",
    "@types/node": "^22.16.5",
    "@types/react": "^19.2.0",
    "@types/react-dom": "^19.2.0",
    "@vitejs/plugin-react": "^5.0.4",
    "eslint": "^9.32.0",
    "eslint-config-prettier": "^10.1.1",
    "eslint-plugin-prettier": "^5.2.6",
    "eslint-plugin-react-hooks": "^5.2.0",
    "eslint-plugin-react-refresh": "^0.4.20",
    "globals": "^15.15.0",
    "prettier": "^3.7.3",
    "typescript": "^5.8.3",
    "typescript-eslint": "^8.56.1",
    "vite": "^7.3.1",
    "vite-tsconfig-paths": "^6.1.1"
  }
}
